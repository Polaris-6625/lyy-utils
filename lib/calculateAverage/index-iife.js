var calculateAverage=function(r){"use strict";return r.calculateAverage=function(r){if(!(r instanceof Array))throw new Error(`${r} is not a Array`);let e=0;return r.map((r=>{e+=r})),e/r.length},r}({});
